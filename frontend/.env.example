# Backend API URL - Required for production deployment
# This should point to your deployed backend API (e.g., on Railway, Render, or your own server)
NEXT_PUBLIC_API_URL=http://localhost:8080

# For local development, use http://localhost:8080
# For production, use your actual backend URL (e.g., https://api.mailmind.com or similar)
